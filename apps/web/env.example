# =============================================================================
# Database
# =============================================================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pmkit?schema=public"

# Redis (for job queue / caching)
REDIS_URL="redis://localhost:6379"

# =============================================================================
# NextAuth (User Authentication)
# =============================================================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# =============================================================================
# SSO OAuth Providers (Teams plan: OIDC)
# =============================================================================

# Google Workspace
# Create at: https://console.cloud.google.com/ → APIs & Services → Credentials
# Redirect URI: https://app.getpmkit.com/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Microsoft Entra ID (Azure AD)
# Create at: https://portal.azure.com/ → App registrations
# Redirect URI: https://app.getpmkit.com/api/auth/callback/azure-ad
AZURE_AD_CLIENT_ID=""
AZURE_AD_CLIENT_SECRET=""
AZURE_AD_TENANT_ID="common"

# =============================================================================
# Stripe Billing
# =============================================================================
# Get keys at: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Create Products & Prices in Stripe Dashboard:
# - Teams Plan: $900/year per seat (annual, per_unit)
STRIPE_TEAMS_PRICE_ID=""

# =============================================================================
# MCP Connector OAuth Apps
# =============================================================================

# Atlassian (Jira + Confluence)
# Create at: https://developer.atlassian.com/console/myapps/
# Scopes: read:jira-work, write:jira-work, read:confluence-content.all, write:confluence-content
# Redirect URI: https://app.getpmkit.com/api/connectors/atlassian/callback
ATLASSIAN_CLIENT_ID=""
ATLASSIAN_CLIENT_SECRET=""

# Slack
# Create at: https://api.slack.com/apps
# Scopes: channels:history, channels:read, chat:write, users:read, search:read
# Redirect URI: https://app.getpmkit.com/api/connectors/slack/callback
SLACK_CLIENT_ID=""
SLACK_CLIENT_SECRET=""
SLACK_SIGNING_SECRET=""

# Gong
# Contact Gong for API access or use: https://developer.gong.io/
# Redirect URI: https://app.getpmkit.com/api/connectors/gong/callback
GONG_CLIENT_ID=""
GONG_CLIENT_SECRET=""

# Zendesk
# Create at: Admin Center → Apps and integrations → APIs → OAuth Clients
# Redirect URI: https://app.getpmkit.com/api/connectors/zendesk/callback
ZENDESK_CLIENT_ID=""
ZENDESK_CLIENT_SECRET=""

# Encryption key for storing connector credentials (generate with: openssl rand -base64 32)
CONNECTOR_ENCRYPTION_KEY=""

# =============================================================================
# Object Storage (S3-compatible)
# =============================================================================
# For artifact storage (PRDs, briefs, meeting packs)
S3_ENDPOINT=""
S3_BUCKET=""
S3_ACCESS_KEY=""
S3_SECRET_KEY=""
S3_REGION="us-east-1"

# =============================================================================
# LLM Provider (OpenAI)
# =============================================================================

# OpenAI API Key (required for production)
# Get at: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# Model selection
# - gpt-5.2: Best quality for production ($1.75/$14 per 1M tokens)
# - gpt-5-mini: Cost-effective for demos ($0.25/$2 per 1M tokens)
LLM_MODEL_DEFAULT="gpt-5.2"
LLM_MODEL_DEMO="gpt-5-mini"

# Demo rate limiting (prevents abuse of public demo)
DEMO_LLM_CALLS_PER_HOUR="10"
DEMO_LLM_CALLS_PER_DAY="50"

# Azure OpenAI (Enterprise BYO LLM - optional)
# For customers who want their own Azure OpenAI endpoint
AZURE_OPENAI_API_KEY=""
AZURE_OPENAI_ENDPOINT=""
AZURE_OPENAI_DEPLOYMENT_NAME=""

# =============================================================================
# Email (Transactional)
# =============================================================================
# For notifications, invites, receipts
RESEND_API_KEY=""
EMAIL_FROM="pmkit <noreply@getpmkit.com>"

# =============================================================================
# Analytics & Monitoring
# =============================================================================

# Simple Analytics (privacy-friendly)
NEXT_PUBLIC_SIMPLE_ANALYTICS_DOMAIN="sa.getpmkit.com"

# Search Console / Webmaster Tools
NEXT_PUBLIC_GSC_VERIFICATION=""
NEXT_PUBLIC_BING_VERIFICATION=""

# PostHog (product analytics - optional)
NEXT_PUBLIC_POSTHOG_KEY=""
NEXT_PUBLIC_POSTHOG_HOST="https://eu.posthog.com"

# Sentry (error tracking - optional)
SENTRY_DSN=""

# =============================================================================
# Feature Flags
# =============================================================================

# Use stub LLM responses (for local development without API key)
# When true: Returns pre-generated responses, no API calls
# When false: Uses real OpenAI API (requires OPENAI_API_KEY)
USE_STUB_LLM="true"

# Use local file storage instead of S3 (for local development only)
USE_LOCAL_STORAGE="true"

# =============================================================================
# Notes on Demo vs Production
# =============================================================================
# 
# LLM Configuration:
# - Demo tenant ("demo"): Uses LLM_MODEL_DEMO (gpt-5-mini) with rate limits
# - Paying customers: Uses LLM_MODEL_DEFAULT (gpt-5.2) for best quality
# - Development: Set USE_STUB_LLM=true for pre-generated responses
#
# Connector Configuration:
# - Demo tenant: ALWAYS uses mock connectors - no OAuth needed
# - Paying customers: Based on ConnectorInstall.status:
#   - status='mock': Uses mock data (OAuth not connected)
#   - status='real': Uses real MCP connection (OAuth connected)
#
# Cost Estimates (per job):
# - gpt-5-mini: ~$0.009/job (demo)
# - gpt-5.2: ~$0.06/job (production)
